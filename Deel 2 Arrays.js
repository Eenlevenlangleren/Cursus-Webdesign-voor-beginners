let selectedColors = ['red', 'green', 'blue'];
let count = selectedColors.push('yellow');
let count = Number.push('5')
let count = Object.push(greeting: 'hi ik ben een object')

console.log (selectedColors);

console.log (selectedColors.length);

let selectedColors = ['red', 'green', 'blue'];
console.log (selectedColors [0]);

let selectedColors = ['red', 'green', 'blue'];
console.log (selectedColors=blue);

let greeting = ('hi, ik ben een object')
console.log(greeting)